const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2 // muito usado para saber se o numero e par ou impar

console.log(soma, subtracao, multiplicacao, divisao, modulo)

// op1 + op2 = operador binário / sintase enfixa
// -Op1 = operador unário / prefix
// op2+ operador unário // posfix
